---
interface Props {
	className?: string;
	delay?: number;
}

const { className = '', delay = 0 } = Astro.props;
---

<div
	class:list={['reveal-motion', className]}
	style={`--reveal-delay: ${delay}s`}
	data-reveal
>
	<slot />
</div>
